---
name: northlit-status
description: Your Northlit account — plan, credits, recent work
---

Report the user's Northlit status:

1. `whoami` — plan, remaining credits, and default project.
2. `list_runs` — the few most recent explorations. Its `mine` list is the user's own work; `sharedWithMe` is boards other people shared with them.
3. Summarize in a couple of sentences: the plan, what's left to spend, what THEY were last working on (`mine[0]` — never a shared board), and that board's openUrl if they want to jump back in. If shared boards exist, mention them separately as "shared with you", with who shared them when known.
